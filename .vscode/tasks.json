{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "cmake --build ${workspaceFolder}/build --config Debug",
            "problemMatcher": [
                "$gcc"
            ],
            "options": {
                "cwd": "${workspaceFolder}"  // Set the working directory to your source folder
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by cmake tools."
        }
    ],
    "version": "2.0.0"
}