#include <vector>

void quickSort(int index[], double dist[], int low, int high);

int giveIndex(std::vector<int> cities, int edge, int n);

void QSort(std::vector<double> &dist, int low, int high);

double findMedian(std::vector<double> a);